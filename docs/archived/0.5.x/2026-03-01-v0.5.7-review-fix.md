# v0.5.7 实现计划

> **Claude 所需子技能**: 使用 superpowers:executing-plans 逐任务实施。

**目标**: 根据三次代码审查结果（UI/UX、代码质量、后端测试架构）进行修复整治，提升代码质量、可维护性和用户体验。

**架构**: 本版本聚焦问题修复与架构优化，分为 UI/UX 交互改进、后端安全与稳定性增强、服务层重构三个方向。采用渐进式重构，保持向后兼容。

**技术栈**: TypeScript, React, Tailwind CSS, Rust (Tauri), Vitest

---

## 修订历史

| 版本   | 日期       | 说明                       |
| ------ | ---------- | -------------------------- |
| v0.5.7 | 2026-03-01 | 初始版本，基于三次审查结果 |

---

## 任务总览

| 方向                              | 任务数 | 优先级 |
| --------------------------------- | ------ | ------ |
| **一、UI/UX 交互改进**            | 5      | P1-P2  |
| **二、后端安全与稳定性**          | 6      | P1     |
| **三、服务层重构 (后端测试架构)** | 9      | P2     |
| **四、性能优化**                  | 3      | P2     |
| **五、无障碍改进**                | 1      | P2     |
| **六、代码质量提升**              | 2      | P3     |
| **七、日志系统**                  | 1      | P2     |

---

## 一、UI/UX 交互改进

### 任务 1: 交互状态补全

**引用**: `docs/plans/todos/ui-ux-review-2026-03-01.md` - P1 (cursor-pointer/hover/focus)

**文件**:

- 修改: `src/app/globals.css`
- 修改: `src/components/ui/` 下所有可交互组件
- 修改: `src/components/features/` 下所有可交互组件

**Step 1: 添加交互工具类**

```css
/* globals.css 添加 */
@layer utilities {
  .interactive {
    @apply cursor-pointer transition-colors duration-200;
  }
  .interactive-hover:hover {
    @apply bg-opacity-80;
  }
  .interactive-focus:focus {
    @apply outline-none ring-2 ring-primary;
  }
}
```

**Step 2: 验证构建**

运行: `npm run build`
预期: 成功，无 CSS 错误

**Step 3: 提交**

```bash
git add src/app/globals.css
git commit -m "feat(ui): add interactive utility classes"
```

---

### 任务 2: 统一 Icons 组件

**引用**: `docs/plans/todos/ui-ux-review-2026-03-01.md` - P1 (内联 SVG 无统一管理)

**文件**:

- 创建: `src/components/ui/Icons/index.tsx`

**Step 1: 创建 Icons 组件**

```tsx
// src/components/ui/Icons/index.tsx
import {
  X,
  Check,
  Plus,
  ChevronDown,
  ChevronUp,
  Search,
  Settings,
  Trash2,
  Edit,
  MoreVertical,
  Menu,
  XCircle,
  CheckCircle,
  Circle,
  Calendar,
  Clock,
  Tag,
  Flag,
} from "lucide-react";

export const Icons = {
  X,
  Check,
  Plus,
  ChevronDown,
  ChevronUp,
  Search,
  Settings,
  Trash2,
  Edit,
  MoreVertical,
  Menu,
  XCircle,
  CheckCircle,
  Circle,
  Calendar,
  Clock,
  Tag,
  Flag,
};
```

**Step 2: 替换内联 SVG**

替换 9 个文件中的 20 处内联 SVG，使用 `Icons.X` 等替代。

**Step 3: 运行 lint**

运行: `npm run lint`
预期: 无 SVG 相关警告

**Step 4: 提交**

```bash
git add src/components/ui/Icons/
git commit -m "feat(ui): create unified Icons component"
```

---

### 任务 3: SearchBar 响应式修复

**引用**:

- `docs/plans/todos/ui-ux-review-2026-03-01.md` - P2 (SearchBar 固定宽度)
- `docs/plans/todos/code-review-findings-2026-03-01.md` - M1 (移动端适配)

**文件**:

- 修改: `src/components/ui/SearchBar.tsx`

**Step 1: 修改宽度样式**

```tsx
// 修改前
className = "w-80 ...";

// 修改后
className = "w-full sm:max-w-xs md:max-w-sm";
```

**Step 2: 响应式调整**

```tsx
className = "w-full sm:max-w-xs md:max-w-sm";
```

**Step 3: 构建验证**

运行: `npm run build`
预期: 构建成功

**Step 4: 提交**

```bash
git add src/components/ui/SearchBar.tsx
git commit -m "fix(ui): make SearchBar responsive"
```

---

### 任务 4: 统一过渡动画时长

**引用**: `docs/plans/todos/ui-ux-review-2026-03-01.md` - P2 (过渡动画不一致)

**文件**:

- 修改: `src/app/globals.css`

**Step 1: 添加 CSS 变量**

```css
:root {
  --transition-fast: 150ms;
  --transition-normal: 200ms;
  --transition-slow: 300ms;
}
```

**Step 2: 统一组件使用**

将 52 处硬编码过渡时长替换为 CSS 变量。

**Step 3: 提交**

```bash
git add src/app/globals.css
git commit -m "feat(ui): standardize transition durations"
```

---

### 任务 5: Error Boundary 组件

**引用**: `docs/plans/todos/ui-ux-review-2026-03-01.md` - P2 (缺少 Error Boundary)

**文件**:

- 创建: `src/components/ui/ErrorBoundary.tsx`
- 修改: `src/app/layout.tsx`

**Step 1: 创建 ErrorBoundary 组件**

```tsx
// src/components/ui/ErrorBoundary.tsx
"use client";

import React from "react";

interface ErrorBoundaryProps {
  children: React.ReactNode;
  fallback?: React.ReactNode;
}

interface ErrorBoundaryState {
  hasError: boolean;
  error?: Error;
}

export class ErrorBoundary extends React.Component<
  ErrorBoundaryProps,
  ErrorBoundaryState
> {
  constructor(props: ErrorBoundaryProps) {
    super(props);
    this.state = { hasError: false };
  }

  static getDerivedStateFromError(error: Error): ErrorBoundaryState {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: React.ErrorInfo) {
    console.error("ErrorBoundary caught:", error, errorInfo);
  }

  render() {
    if (this.state.hasError) {
      return (
        this.props.fallback || (
          <div className="flex items-center justify-center min-h-screen p-4">
            <div className="text-center">
              <h1 className="text-xl font-bold text-red-600">出错了</h1>
              <p className="mt-2 text-gray-600">{this.state.error?.message}</p>
              <button
                onClick={() => this.setState({ hasError: false })}
                className="mt-4 px-4 py-2 bg-primary text-white rounded"
              >
                重试
              </button>
            </div>
          </div>
        )
      );
    }

    return this.props.children;
  }
}
```

**Step 2: 集成到 layout**

```tsx
// src/app/layout.tsx
import { ErrorBoundary } from "@/components/ui/ErrorBoundary";

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="zh-CN">
      <body>
        <ErrorBoundary>{children}</ErrorBoundary>
      </body>
    </html>
  );
}
```

**Step 3: 提交**

```bash
git add src/components/ui/ErrorBoundary.tsx src/app/layout.tsx
git commit -m "feat(ui): add Error Boundary component"
```

---

## 二、后端安全与稳定性 (P1)

### 任务 6: 后端输入验证

**引用**: `docs/plans/todos/code-review-findings-2026-03-01.md` - P1-1 (缺少输入验证)

**文件**:

- 创建: `src-tauri/src/commands/validation.rs`
- 修改: `src-tauri/src/commands/todos.rs`
- 修改: `src-tauri/src/commands/plans.rs`
- 修改: `src-tauri/src/commands/targets.rs`
- 修改: `src-tauri/src/commands/milestones.rs`
- 修改: `src-tauri/src/commands/circulations.rs`

**Step 1: 创建 validation.rs**

```rust
// src-tauri/src/commands/validation.rs

pub fn validate_title(title: &str) -> Result<(), String> {
    if title.trim().is_empty() {
        return Err("Title cannot be empty".to_string());
    }
    if title.len() > 500 {
        return Err("Title exceeds maximum length of 500 characters".to_string());
    }
    Ok(())
}

pub fn validate_priority(priority: &str) -> Result<(), String> {
    if !["P0", "P1", "P2", "P3"].contains(&priority) {
        return Err("Invalid priority value".to_string());
    }
    Ok(())
}

pub fn validate_status(status: &str) -> Result<(), String> {
    let valid_statuses = ["pending", "in_progress", "completed", "cancelled"];
    if !valid_statuses.contains(&status) {
        return Err(format!("Invalid status: {}", status));
    }
    Ok(())
}
```

**Step 2: 在 todos.rs 中应用验证**

```rust
// src-tauri/src/commands/todos.rs
mod validation;

#[tauri::command]
pub fn create_todo(title: String, priority: String, ...) -> Result<Todo, String> {
    validation::validate_title(&title)?;
    validation::validate_priority(&priority)?;
    // 现有逻辑...
}

#[tauri::command]
pub fn update_todo(id: String, title: Option<String>, ...) -> Result<Todo, String> {
    if let Some(ref t) = title {
        validation::validate_title(t)?;
    }
    // 现有逻辑...
}
```

**Step 3: 编译验证**

运行: `cd src-tauri && cargo build`
预期: 编译成功

**Step 4: 提交**

```bash
git add src-tauri/src/commands/
git commit -m "feat(backend): add input validation to CRUD commands"
```

---

### 任务 7: 修复静默错误吞没

**引用**: `docs/plans/todos/code-review-findings-2026-03-01.md` - P1-2 (静默错误吞没)

**文件**:

- 修改: `src-tauri/src/db.rs`

**Step 1: 定位 .ok() 调用**

找到约 6 处 `.ok()` 静默吞没错误的位置。

**Step 2: 修改为错误日志**

```rust
// 修改前:
conn.execute(...).ok();

// 修改后:
if let Err(e) = conn.execute(...) {
    log::warn!("Migration note: {}", e);
}
```

**Step 3: 编译验证**

运行: `cd src-tauri && cargo build`
预期: 编译成功

**Step 4: 提交**

```bash
git add src-tauri/src/db.rs
git commit -m "fix(backend): log migration errors instead of silently swallowing"
```

---

### 任务 8: 导入操作事务包装

**引用**: `docs/plans/todos/code-review-findings-2026-03-01.md` - P1-3 (缺少事务包装)

**文件**:

- 修改: `src-tauri/src/commands/import.rs`

**Step 1: 为 merge 模式添加事务**

```rust
fn import_merge(
    conn: &rusqlite::Connection,
    data: &ExportDataContent,
) -> Result<ImportResult, String> {
    let tx = conn.unchecked_transaction()?;

    // ... 所有操作 ...

    tx.commit()?;
    Ok(result)
}
```

**Step 2: 为 replace 模式添加事务**

```rust
fn import_replace(...) -> Result<ImportResult, String> {
    let tx = conn.unchecked_transaction()?;

    // 清空表
    tx.execute("DELETE FROM entity_tags", [])?;
    tx.execute("DELETE FROM milestones", [])?;
    // ... 其他表 ...

    // 导入数据
    // ...

    tx.commit()?;
    Ok(result)
}
```

**Step 3: 为 update 模式添加事务**

同上。

**Step 4: 编译验证**

运行: `cd src-tauri && cargo build`
预期: 编译成功

**Step 5: 提交**

```bash
git add src-tauri/src/commands/import.rs
git commit -m "fix(backend): wrap import operations in transactions"
```

---

### 任务 9: 修复 SQL 注入风险

**引用**: `docs/plans/todos/code-review-findings-2026-03-01.md` - F1, B1 (SQL 注入风险)

**文件**:

- 修改: `src-tauri/src/commands/search.rs`

**Step 1: 使用参数化查询**

将 `format!("%{}%", query)` 改为参数化查询。

**Step 2: 编译验证**

运行: `cd src-tauri && cargo build`
预期: 编译成功

**Step 3: 提交**

```bash
git add src-tauri/src/commands/search.rs
git commit -m "fix(backend): use parameterized queries to prevent SQL injection"
```

---

### 任务 10: 移除 dangerouslySetInnerHTML

**引用**: `docs/plans/todos/code-review-findings-2026-03-01.md` - F2 (XSS 风险)

**文件**:

- 修改: `src/app/layout.tsx`

**Step 1: 评估必要性**

检查主题脚本是否必需，如非必要则移除。

**Step 2: 如需保留，确保安全**

如无法移除，确保数据来源可信。

**Step 3: 提交**

```bash
git add src/app/layout.tsx
git commit -m "fix(security): remove or secure dangerouslySetInnerHTML usage"
```

---

### 任务 11: React Query 配置

**引用**: `docs/plans/todos/code-review-findings-2026-03-01.md` - F5 (React Query 未配置)

**文件**:

- 修改: `src/app/providers.tsx`

**Step 1: 添加全局错误处理和重试配置**

```tsx
// src/app/providers.tsx
"use client";

import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { QueryErrorResetBoundary } from "@tanstack/react-query-devtools";
import { useState } from "react";

export function Providers({ children }: { children: React.ReactNode }) {
  const [queryClient] = useState(
    () =>
      new QueryClient({
        defaultOptions: {
          queries: {
            retry: 3,
            staleTime: 5 * 60 * 1000, // 5 minutes
            gcTime: 10 * 60 * 1000, // 10 minutes (formerly cacheTime)
          },
        },
      }),
  );

  return (
    <QueryClientProvider client={queryClient}>
      <QueryErrorResetBoundary>{children}</QueryErrorResetBoundary>
    </QueryClientProvider>
  );
}
```

**Step 2: 提交**

```bash
git add src/app/providers.tsx
git commit -m "feat(frontend): configure React Query with retry and error handling"
```

---

## 三、服务层重构 (后端测试架构)

> **引用**: `docs/plans/todos/backend-test-architecture-redesign-2026-03-01.md` - 完整文档

此部分为后端测试架构重构的核心内容，将业务逻辑从 Tauri API 耦合中解耦，实现可测试性。

### 任务 12: 创建目录结构

**引用**: `docs/plans/todos/backend-test-architecture-redesign-2026-03-01.md` - 服务层架构

**文件**:

- 创建: `src/lib/services/index.ts`

**Step 1: 创建目录结构**

```bash
mkdir -p src/lib/services
```

**Step 2: 创建 index.ts**

```typescript
// src/lib/services/index.ts
export * from "./validation";
export * from "./planService";
export * from "./todoService";
export * from "./circulationService";
export * from "./targetService";
export * from "./milestoneService";
```

**Step 3: 提交**

```bash
git add src/lib/services/
git commit -m "refactor(backend): create services directory structure"
```

---

### 任务 13: validation.ts 验证层

**引用**:

- `docs/plans/todos/backend-test-architecture-redesign-2026-03-01.md` - 验证设计
- `docs/plans/todos/code-review-findings-2026-03-01.md` - A3 (缺少 Zod 验证)

**文件**:

- 创建: `src/lib/services/validation.ts`
- 创建: `src/lib/services/validation.test.ts`

**Step 1: 创建 validation.ts**

```typescript
// src/lib/services/validation.ts
export interface ValidationError {
  field: string;
  message: string;
}

export function required(
  value: unknown,
  fieldName: string,
): ValidationError | null {
  if (value === null || value === undefined || value === "") {
    return { field: fieldName, message: `${fieldName}不能为空` };
  }
  return null;
}

export function maxLength(
  value: string,
  max: number,
  fieldName: string,
): ValidationError | null {
  if (value && value.length > max) {
    return { field: fieldName, message: `${fieldName}不能超过${max}个字符` };
  }
  return null;
}

export function validatePriority(priority: string): ValidationError | null {
  if (!["P0", "P1", "P2", "P3"].includes(priority)) {
    return { field: "priority", message: "无效的优先级" };
  }
  return null;
}

export function validateStatus(status: string): ValidationError | null {
  const validStatuses = ["pending", "in_progress", "completed", "cancelled"];
  if (!validStatuses.includes(status)) {
    return { field: "status", message: "无效的状态" };
  }
  return null;
}

// 领域验证器
export function validateTodo(data: {
  title?: string;
  priority?: string;
  status?: string;
}): ValidationError[] {
  const errors: ValidationError[] = [];

  if (data.title !== undefined) {
    const titleError = required(data.title, "title");
    if (!titleError) {
      const lengthError = maxLength(data.title, 500, "title");
      if (lengthError) errors.push(lengthError);
    } else {
      errors.push(titleError);
    }
  }

  if (data.priority !== undefined) {
    const priorityError = validatePriority(data.priority);
    if (priorityError) errors.push(priorityError);
  }

  if (data.status !== undefined) {
    const statusError = validateStatus(data.status);
    if (statusError) errors.push(statusError);
  }

  return errors;
}
```

**Step 2: 创建测试文件**

```typescript
// src/lib/services/validation.test.ts
import { describe, it, expect } from "vitest";
import {
  required,
  maxLength,
  validatePriority,
  validateTodo,
} from "./validation";

describe("validation", () => {
  describe("required", () => {
    it("should return error for null", () => {
      expect(required(null, "title")).toEqual({
        field: "title",
        message: "title不能为空",
      });
    });

    it("should return error for undefined", () => {
      expect(required(undefined, "title")).toEqual({
        field: "title",
        message: "title不能为空",
      });
    });

    it("should return error for empty string", () => {
      expect(required("", "title")).toEqual({
        field: "title",
        message: "title不能为空",
      });
    });

    it("should return null for valid value", () => {
      expect(required("test", "title")).toBeNull();
    });
  });

  describe("maxLength", () => {
    it("should return error when exceeding max length", () => {
      expect(maxLength("a".repeat(501), 500, "title")).toEqual({
        field: "title",
        message: "title不能超过500个字符",
      });
    });

    it("should return null for valid length", () => {
      expect(maxLength("a".repeat(500), 500, "title")).toBeNull();
    });
  });

  describe("validatePriority", () => {
    it("should return error for invalid priority", () => {
      expect(validatePriority("P5")).toEqual({
        field: "priority",
        message: "无效的优先级",
      });
    });

    it("should return null for valid priority", () => {
      expect(validatePriority("P1")).toBeNull();
    });
  });

  describe("validateTodo", () => {
    it("should validate complete todo data", () => {
      const errors = validateTodo({
        title: "",
        priority: "P5",
        status: "invalid",
      });
      expect(errors.length).toBeGreaterThan(0);
    });
  });
});
```

**Step 3: 运行测试**

运行: `npm run test -- src/lib/services/validation.test.ts`
预期: 所有测试通过

**Step 4: 提交**

```bash
git add src/lib/services/validation.ts src/lib/services/validation.test.ts
git commit -m "feat(services): add validation.ts with generic validators"
```

---

### 任务 14: planService.ts

**引用**: `docs/plans/todos/backend-test-architecture-redesign-2026-03-01.md` - 服务设计

**文件**:

- 创建: `src/lib/services/planService.ts`
- 创建: `src/lib/services/planService.test.ts`

**Step 1: 创建 planService.ts**

```typescript
// src/lib/services/planService.ts
export interface Plan {
  id: string;
  title: string;
  deadline?: string;
  steps?: Step[];
  status?: string;
}

export interface Step {
  id: string;
  status: "pending" | "in_progress" | "completed";
}

export interface PlanProgress {
  completed: number;
  total: number;
  percentage: number;
}

export function calculatePlanProgress(steps: Step[]): PlanProgress {
  if (!steps || steps.length === 0) {
    return { completed: 0, total: 0, percentage: 0 };
  }
  const completed = steps.filter((s) => s.status === "completed").length;
  const total = steps.length;
  const percentage = Math.round((completed / total) * 100);
  return { completed, total, percentage };
}

export function sortPlansByDeadline(plans: Plan[]): Plan[] {
  return [...plans].sort((a, b) => {
    if (!a.deadline && !b.deadline) return 0;
    if (!a.deadline) return 1;
    if (!b.deadline) return -1;
    return new Date(a.deadline).getTime() - new Date(b.deadline).getTime();
  });
}

export function filterPlansByStatus(plans: Plan[], status: string): Plan[] {
  return plans.filter((p) => p.status === status);
}

export function getOverduePlans(plans: Plan[]): Plan[] {
  const now = new Date();
  return plans.filter((p) => {
    if (!p.deadline) return false;
    return new Date(p.deadline) < now;
  });
}
```

**Step 2: 创建测试文件**

```typescript
// src/lib/services/planService.test.ts
import { describe, it, expect } from "vitest";
import {
  calculatePlanProgress,
  sortPlansByDeadline,
  filterPlansByStatus,
  getOverduePlans,
} from "./planService";

describe("planService", () => {
  describe("calculatePlanProgress", () => {
    it("should return zero for empty steps", () => {
      const result = calculatePlanProgress([]);
      expect(result).toEqual({ completed: 0, total: 0, percentage: 0 });
    });

    it("should calculate correct progress", () => {
      const steps = [
        { id: "1", status: "completed" },
        { id: "2", status: "pending" },
        { id: "3", status: "completed" },
        { id: "4", status: "pending" },
      ];
      const result = calculatePlanProgress(steps);
      expect(result.completed).toBe(2);
      expect(result.total).toBe(4);
      expect(result.percentage).toBe(50);
    });
  });

  describe("sortPlansByDeadline", () => {
    it("should sort plans by deadline", () => {
      const plans = [
        { id: "1", title: "Plan A", deadline: "2026-03-10" },
        { id: "2", title: "Plan B", deadline: "2026-03-01" },
        { id: "3", title: "Plan C", deadline: "2026-03-15" },
      ];
      const result = sortPlansByDeadline(plans);
      expect(result[0].id).toBe("2");
      expect(result[1].id).toBe("1");
      expect(result[2].id).toBe("3");
    });
  });
});
```

**Step 3: 运行测试**

运行: `npm run test -- src/lib/services/planService.test.ts`
预期: 所有测试通过

**Step 4: 提交**

```bash
git add src/lib/services/planService.ts src/lib/services/planService.test.ts
git commit -m "feat(services): add planService with business logic"
```

---

### 任务 15: todoService.ts

**引用**: `docs/plans/todos/backend-test-architecture-redesign-2026-03-01.md` - 服务设计

**文件**:

- 创建: `src/lib/services/todoService.ts`
- 创建: `src/lib/services/todoService.test.ts`

**Step 1: 创建 todoService.ts**

```typescript
// src/lib/services/todoService.ts
export interface Todo {
  id: string;
  title: string;
  priority?: string;
  status?: string;
  tags?: string[];
  dueDate?: string;
}

export function filterTodosByPriority(todos: Todo[], priority: string): Todo[] {
  return todos.filter((t) => t.priority === priority);
}

export function groupTodosByStatus(todos: Todo[]): Record<string, Todo[]> {
  return todos.reduce(
    (acc, todo) => {
      const status = todo.status || "pending";
      if (!acc[status]) acc[status] = [];
      acc[status].push(todo);
      return acc;
    },
    {} as Record<string, Todo[]>,
  );
}

export function filterTodosByTag(todos: Todo[], tag: string): Todo[] {
  return todos.filter((t) => t.tags?.includes(tag));
}

export function getTodosDueSoon(todos: Todo[], days: number = 3): Todo[] {
  const now = new Date();
  const future = new Date(now.getTime() + days * 24 * 60 * 60 * 1000);
  return todos.filter((t) => {
    if (!t.dueDate) return false;
    const dueDate = new Date(t.dueDate);
    return dueDate >= now && dueDate <= future;
  });
}
```

**Step 2: 创建测试文件**

```typescript
// src/lib/services/todoService.test.ts
import { describe, it, expect } from "vitest";
import {
  filterTodosByPriority,
  groupTodosByStatus,
  filterTodosByTag,
  getTodosDueSoon,
} from "./todoService";

describe("todoService", () => {
  describe("filterTodosByPriority", () => {
    it("should filter todos by priority", () => {
      const todos = [
        { id: "1", title: "Todo 1", priority: "P1" },
        { id: "2", title: "Todo 2", priority: "P2" },
        { id: "3", title: "Todo 3", priority: "P1" },
      ];
      const result = filterTodosByPriority(todos, "P1");
      expect(result.length).toBe(2);
    });
  });

  describe("groupTodosByStatus", () => {
    it("should group todos by status", () => {
      const todos = [
        { id: "1", title: "Todo 1", status: "pending" },
        { id: "2", title: "Todo 2", status: "completed" },
        { id: "3", title: "Todo 3", status: "pending" },
      ];
      const result = groupTodosByStatus(todos);
      expect(result.pending?.length).toBe(2);
      expect(result.completed?.length).toBe(1);
    });
  });
});
```

**Step 3: 运行测试**

运行: `npm run test -- src/lib/services/todoService.test.ts`
预期: 所有测试通过

**Step 4: 提交**

```bash
git add src/lib/services/todoService.ts src/lib/services/todoService.test.ts
git commit -m "feat(services): add todoService with business logic"
```

---

### 任务 16: circulationService.ts

**引用**: `docs/plans/todos/backend-test-architecture-redesign-2026-03-01.md` - 服务设计

**文件**:

- 创建: `src/lib/services/circulationService.ts`
- 创建: `src/lib/services/circulationService.test.ts`

**Step 1: 创建 circulationService.ts**

```typescript
// src/lib/services/circulationService.ts
export interface CirculationLog {
  id: string;
  count: number;
  date: string;
}

export interface CirculationStats {
  totalCount: number;
  avgCount: number;
}

export function calculateCirculationStats(
  logs: CirculationLog[],
): CirculationStats {
  if (logs.length === 0) {
    return { totalCount: 0, avgCount: 0 };
  }
  const totalCount = logs.reduce((sum, log) => sum + log.count, 0);
  const avgCount = totalCount / logs.length;
  return { totalCount, avgCount };
}

export function getCirculationTrend(
  logs: CirculationLog[],
  days: number = 7,
): number[] {
  const now = new Date();
  const result: number[] = [];
  for (let i = days - 1; i >= 0; i--) {
    const date = new Date(now.getTime() - i * 24 * 60 * 60 * 1000);
    const dateStr = date.toISOString().split("T")[0];
    const dayLog = logs.find((l) => l.date.startsWith(dateStr));
    result.push(dayLog?.count || 0);
  }
  return result;
}
```

**Step 2: 创建测试文件**

```typescript
// src/lib/services/circulationService.test.ts
import { describe, it, expect } from "vitest";
import {
  calculateCirculationStats,
  getCirculationTrend,
} from "./circulationService";

describe("circulationService", () => {
  describe("calculateCirculationStats", () => {
    it("should return zero for empty logs", () => {
      const result = calculateCirculationStats([]);
      expect(result).toEqual({ totalCount: 0, avgCount: 0 });
    });

    it("should calculate correct stats", () => {
      const logs = [
        { id: "1", count: 10, date: "2026-03-01" },
        { id: "2", count: 20, date: "2026-03-02" },
        { id: "3", count: 30, date: "2026-03-03" },
      ];
      const result = calculateCirculationStats(logs);
      expect(result.totalCount).toBe(60);
      expect(result.avgCount).toBe(20);
    });
  });
});
```

**Step 3: 运行测试**

运行: `npm run test -- src/lib/services/circulationService.test.ts`
预期: 所有测试通过

**Step 4: 提交**

```bash
git add src/lib/services/circulationService.ts src/lib/services/circulationService.test.ts
git commit -m "feat(services): add circulationService with business logic"
```

---

### 任务 17: targetService.ts

**引用**: `docs/plans/todos/backend-test-architecture-redesign-2026-03-01.md` - 服务设计

**文件**:

- 创建: `src/lib/services/targetService.ts`
- 创建: `src/lib/services/targetService.test.ts`

**Step 1: 创建 targetService.ts**

```typescript
// src/lib/services/targetService.ts
export interface Target {
  id: string;
  title: string;
  current: number;
  target: number;
}

export interface TargetProgress {
  id: string;
  progress: number;
  isCompleted: boolean;
}

export function sortTargetsByProgress(targets: Target[]): Target[] {
  return [...targets].sort((a, b) => {
    const aProgress = a.target > 0 ? a.current / a.target : 0;
    const bProgress = b.target > 0 ? b.current / b.target : 0;
    return bProgress - aProgress;
  });
}

export function calculateTargetProgress(target: Target): TargetProgress {
  const progress =
    target.target > 0 ? (target.current / target.target) * 100 : 0;
  return {
    id: target.id,
    progress: Math.round(progress),
    isCompleted: target.current >= target.target,
  };
}

export function getCompletedTargets(targets: Target[]): Target[] {
  return targets.filter((t) => t.current >= t.target);
}

export function getOverdueTargets(targets: Target[]): Target[] {
  return targets.filter((t) => t.current < t.target);
}
```

**Step 2: 创建测试文件**

```typescript
// src/lib/services/targetService.test.ts
import { describe, it, expect } from "vitest";
import {
  sortTargetsByProgress,
  calculateTargetProgress,
  getCompletedTargets,
} from "./targetService";

describe("targetService", () => {
  describe("sortTargetsByProgress", () => {
    it("should sort targets by progress descending", () => {
      const targets = [
        { id: "1", title: "Target 1", current: 30, target: 100 },
        { id: "2", title: "Target 2", current: 80, target: 100 },
        { id: "3", title: "Target 3", current: 50, target: 100 },
      ];
      const result = sortTargetsByProgress(targets);
      expect(result[0].id).toBe("2");
      expect(result[1].id).toBe("3");
      expect(result[2].id).toBe("1");
    });
  });

  describe("calculateTargetProgress", () => {
    it("should calculate correct progress", () => {
      const target = { id: "1", title: "Target", current: 75, target: 100 };
      const result = calculateTargetProgress(target);
      expect(result.progress).toBe(75);
      expect(result.isCompleted).toBe(false);
    });
  });
});
```

**Step 3: 运行测试**

运行: `npm run test -- src/lib/services/targetService.test.ts`
预期: 所有测试通过

**Step 4: 提交**

```bash
git add src/lib/services/targetService.ts src/lib/services/targetService.test.ts
git commit -m "feat(services): add targetService with business logic"
```

---

### 任务 18: milestoneService.ts

**引用**: `docs/plans/todos/backend-test-architecture-redesign-2026-03-01.md` - 服务设计

**文件**:

- 创建: `src/lib/services/milestoneService.ts`
- 创建: `src/lib/services/milestoneService.test.ts`

**Step 1: 创建 milestoneService.ts**

```typescript
// src/lib/services/milestoneService.ts
export interface Milestone {
  id: string;
  title: string;
  status?: string;
  dueDate?: string;
}

export function filterMilestonesByStatus(
  milestones: Milestone[],
  status: string,
): Milestone[] {
  return milestones.filter((m) => m.status === status);
}

export function sortMilestonesByDueDate(milestones: Milestone[]): Milestone[] {
  return [...milestones].sort((a, b) => {
    if (!a.dueDate && !b.dueDate) return 0;
    if (!a.dueDate) return 1;
    if (!b.dueDate) return -1;
    return new Date(a.dueDate).getTime() - new Date(b.dueDate).getTime();
  });
}

export function getUpcomingMilestones(
  milestones: Milestone[],
  days: number = 7,
): Milestone[] {
  const now = new Date();
  const future = new Date(now.getTime() + days * 24 * 60 * 60 * 1000);
  return milestones.filter((m) => {
    if (!m.dueDate) return false;
    const dueDate = new Date(m.dueDate);
    return dueDate >= now && dueDate <= future;
  });
}
```

**Step 2: 创建测试文件**

```typescript
// src/lib/services/milestoneService.test.ts
import { describe, it, expect } from "vitest";
import {
  filterMilestonesByStatus,
  sortMilestonesByDueDate,
  getUpcomingMilestones,
} from "./milestoneService";

describe("milestoneService", () => {
  describe("filterMilestonesByStatus", () => {
    it("should filter milestones by status", () => {
      const milestones = [
        { id: "1", title: "M1", status: "completed" },
        { id: "2", title: "M2", status: "pending" },
        { id: "3", title: "M3", status: "completed" },
      ];
      const result = filterMilestonesByStatus(milestones, "completed");
      expect(result.length).toBe(2);
    });
  });

  describe("sortMilestonesByDueDate", () => {
    it("should sort milestones by due date", () => {
      const milestones = [
        { id: "1", title: "M1", dueDate: "2026-03-10" },
        { id: "2", title: "M2", dueDate: "2026-03-01" },
        { id: "3", title: "M3", dueDate: "2026-03-15" },
      ];
      const result = sortMilestonesByDueDate(milestones);
      expect(result[0].id).toBe("2");
      expect(result[1].id).toBe("1");
      expect(result[2].id).toBe("3");
    });
  });
});
```

**Step 3: 运行测试**

运行: `npm run test -- src/lib/services/milestoneService.test.ts`
预期: 所有测试通过

**Step 4: 提交**

```bash
git add src/lib/services/milestoneService.ts src/lib/services/milestoneService.test.ts
git commit -m "feat(services): add milestoneService with business logic"
```

---

### 任务 19: api.ts 重构

**引用**: `docs/plans/todos/backend-test-architecture-redesign-2026-03-01.md` - API 层重构

**文件**:

- 修改: `src/lib/api.ts`

**Step 1: 导出服务层**

```typescript
// src/lib/api.ts 追加导出
import * as planService from "./services/planService";
import * as todoService from "./services/todoService";
import * as circulationService from "./services/circulationService";
import * as targetService from "./services/targetService";
import * as milestoneService from "./services/milestoneService";
import * as validation from "./services/validation";

export const services = {
  plan: planService,
  todo: todoService,
  circulation: circulationService,
  target: targetService,
  milestone: milestoneService,
  validation,
};
```

**Step 2: 提交**

```bash
git add src/lib/api.ts
git commit -m "refactor(frontend): integrate services into api layer"
```

---

### 任务 20: Zustand Store 移除

**引用**: `docs/plans/todos/code-review-findings-2026-03-01.md` - F8 (Zustand store 死代码)

**文件**:

- 删除: `src/lib/store.ts`

**Step 1: 确认无引用**

确认代码中无 `useAppStore` 引用。

**Step 2: 删除文件**

```bash
rm src/lib/store.ts
```

**Step 3: 提交**

```bash
git rm src/lib/store.ts
git commit -m "chore: remove unused Zustand store (dead code)"
```

---

## 四、性能优化

### 任务 21: N+1 查询修复

**引用**: `docs/plans/todos/code-review-findings-2026-03-01.md` - P1 (性能问题)

**文件**:

- 修改: `src/hooks/useTodos.ts`

**Step 1: 修改 hook**

直接调用 `getTodo(id)` 而非获取全部后过滤。

**Step 2: 测试验证**

运行: `npm run test -- src/hooks/`
预期: 相关测试通过

**Step 3: 提交**

```bash
git add src/hooks/useTodos.ts
git commit -m "perf(frontend): fix N+1 query in useTodos"
```

---

### 任务 22: React.memo 优化

**引用**: `docs/plans/todos/code-review-findings-2026-03-01.md` - P3 (TodoItem 缺少 React.memo)

**文件**:

- 修改: `src/components/TodoItem.tsx`

**Step 1: 包装组件**

```tsx
export const TodoItem = React.memo(function TodoItem({ ... }) {
  // ...
});
```

**Step 2: 构建验证**

运行: `npm run build`
预期: 构建成功

**Step 3: 提交**

```bash
git add src/components/TodoItem.tsx
git commit -m "perf(frontend): add React.memo to TodoItem"
```

---

### 任务 23: CirculationList 虚拟化 (可选)

**引用**: `docs/plans/todos/code-review-findings-2026-03-01.md` - P4 (CirculationList 无虚拟化)

**文件**:

- 修改: `src/components/CirculationList.tsx`

**Step 1: 考虑使用 react-window**

评估是否需要虚拟滚动。

**Step 2: 如需实现**

```bash
npm install react-window
```

---

## 五、无障碍改进

### 任务 24: 无障碍改进

**引用**:

- `docs/plans/todos/code-review-findings-2026-03-01.md` - A1, A2, A3 (无障碍访问)
- `docs/plans/todos/ui-ux-review-2026-03-01.md` - 响应式问题

**文件**:

- 修改: `src/components/ui/IconButton.tsx`
- 修改: `src/components/ui/BottomNav.tsx`
- 修改: `src/components/features/TodoItem.tsx`
- 修改: `src/app/layout.tsx`

**Step 1: 添加 aria-label**

```tsx
// IconButton.tsx
<button aria-label={label} ...>
```

```tsx
// BottomNav.tsx
<button aria-label={t('nav.dashboard')} ...>
```

**Step 2: 添加键盘导航**

```tsx
// TodoItem.tsx
<div
  onClick={handleClick}
  onKeyDown={(e) => e.key === 'Enter' && handleClick()}
  role="button"
  tabIndex={0}
>
```

**Step 3: 移除 viewport 缩放限制**

```tsx
// layout.tsx
export const viewport = {
  maximumScale: 5,
  userScalable: true,
};
```

**Step 4: 提交**

```bash
git add src/components/ui/IconButton.tsx src/components/ui/BottomNav.tsx src/components/features/TodoItem.tsx src/app/layout.tsx
git commit -m "feat(a11y): add aria-labels and keyboard navigation"
```

---

## 六、代码质量提升

### 任务 25: 清理 eslint-disable

**引用**: `docs/plans/todos/code-review-findings-2026-03-01.md` - F7 (eslint-disable 滥用)

**文件**:

- 修改: 13 个文件

**Step 1: 移除 eslint-disable**

定位并修复或移除 13 处 eslint-disable 注释。

**Step 2: 运行 lint**

运行: `npm run lint`
预期: 无错误

**Step 3: 提交**

```bash
git add src/
git commit -m "chore: remove eslint-disable comments"
```

---

### 任务 26: 扩展日志系统

**引用**: `docs/plans/todos/code-review-findings-2026-03-01.md` - L1, L2, L3 (日志系统)

**文件**:

- 修改: `src-tauri/src/commands/` 下所有命令文件

**Step 1: 添加日志到所有 Rust 命令**

为每个命令添加 `log::info!` / `log::error!` 调用。

**Step 2: 编译验证**

运行: `cd src-tauri && cargo build`
预期: 编译成功

**Step 3: 提交**

```bash
git add src-tauri/src/commands/
git commit -m "feat(logging): add logging to all Rust command handlers"
```

---

## 验证清单

完成所有任务后，执行以下验证：

- [ ] `npm run test` - 所有测试通过
- [ ] `npm run typecheck` - 无类型错误
- [ ] `npm run lint` - 无 lint 错误
- [ ] `npm run format` - 代码已格式化
- [ ] `npm run build` - 构建成功
- [ ] `npm run test:coverage` - 覆盖率 >= 90%

---

## 相关文档

- [UI/UX 审查报告](./todos/ui-ux-review-2026-03-01.md)
- [代码审查发现](./todos/code-review-findings-2026-03-01.md)
- [后端测试架构重构设计](./todos/backend-test-architecture-redesign-2026-03-01.md)
