# v0.5.4 激进重构设计 - React Query + Zustand

> 创建日期：2026-02-23
> 状态：待审批
> 版本：v0.5.4

---

## 概述

本次迭代采用激进优化方案，引入现代 React 生态库，全面重构应用架构。

### 核心目标

1. **数据获取** - 使用 React Query 替代手动的 useEffect + useState
2. **状态管理** - 使用 Zustand 集中管理应用状态
3. **API 层** - 全面重构，添加缓存/重试/乐观更新
4. **移动端 Bug** - 修复 4 个遗留问题

### 技术选型

| 领域 | 方案 | 理由 |
|------|------|------|
| 数据获取 | TanStack Query v5 | React 官方推荐，功能强大 |
| 状态管理 | Zustand | 轻量、简单、TypeScript 友好 |
| 乐观更新 | React Query 内置 | 无需额外工作 |

---

## 问题分析

### 1. React 反模式：setState in useEffect

**当前问题**：12 处使用 `isLoaded.current` 防止重复加载

```tsx
// ❌ 反模式
useEffect(() => {
  if (isLoaded.current) return;
  isLoaded.current = true;
  loadData();
}, []);
```

**为什么有问题**：
- React 官方推荐使用数据获取库
- 手动管理加载状态容易出错
- 缺乏缓存、去重、预获取等能力

### 2. 状态管理混乱

**当前问题**：
- 状态分散在各个组件中
- 组件间状态共享困难
- 缺乏全局状态管理

### 3. API 层缺乏健壮性

**当前问题**：
- 无请求缓存
- 无错误重试
- 无乐观更新
- 网络错误处理不一致

### 4. 移动端 Bug

| Bug | 描述 |
|-----|------|
| #1 | 汉堡菜单 + 应用标题与 body 顶部重叠 |
| #2 | Sidebar 遮罩层遮挡 |
| #3 | APK 应用图标仍是默认 |
| #4 | Sidebar 菜单无法点击，点击就缩回 |

---

## 架构设计

### 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        UI Layer                              │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐    │
│  │Dashboard│  │  Todos  │  │  Plans  │  │  ...   │    │
│  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘    │
└───────┼────────────┼────────────┼────────────┼─────────────┘
        │            │            │            │
        ▼            ▼            ▼            ▼
┌─────────────────────────────────────────────────────────────┐
│                    React Query Layer                        │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  useQuery / useMutation / 缓存 / 预获取 / 乐观更新  │  │
│  └─────────────────────────────────────────────────────┘  │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    API Layer                                │
│  ┌─────────────────────────────────────────────────────┐  │
│  │              api.ts (重构后)                        │  │
│  │  - 类型安全                                         │  │
│  │  - 错误处理                                         │  │
│  │  - 请求/响应拦截                                    │  │
│  └─────────────────────────────────────────────────────┘  │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    Tauri IPC                                │
│  ┌─────────────────────────────────────────────────────┐  │
│  │              invoke() / Tauri Commands             │  │
│  └─────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### Zustand Store 设计

```typescript
// src/stores/appStore.ts
import { create } from 'zustand';

interface AppState {
  // UI State
  sidebarOpen: boolean;
  toggleSidebar: () => void;
  closeSidebar: () => void;
  
  // Theme State
  theme: ThemeId;
  setTheme: (theme: ThemeId) => void;
  
  // Filters (全局筛选状态)
  selectedTags: string[];
  setSelectedTags: (tags: string[]) => void;
  clearFilters: () => void;
}

export const useAppStore = create<AppState>((set) => ({
  sidebarOpen: false,
  toggleSidebar: () => set((state) => ({ sidebarOpen: !state.sidebarOpen })),
  closeSidebar: () => set({ sidebarOpen: false }),
  
  theme: 'light',
  setTheme: (theme) => set({ theme }),
  
  selectedTags: [],
  setSelectedTags: (tags) => set({ selectedTags: tags }),
  clearFilters: () => set({ selectedTags: [] }),
}));
```

### React Query 集成

```typescript
// src/lib/query.ts
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';

export const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 5 * 60 * 1000, // 5 分钟
      gcTime: 10 * 60 * 1000, // 10 分钟
      retry: 2,
      refetchOnWindowFocus: false,
    },
  },
});

// 使用示例
function Dashboard() {
  // ❌ 之前
  const [data, setData] = useState(null);
  useEffect(() => { loadData().then(setData); }, []);
  
  // ✅ 之后
  const { data, isLoading, error, refetch } = useQuery({
    queryKey: ['dashboard'],
    queryFn: getDashboard,
  });
  
  if (isLoading) return <Skeleton />;
  if (error) return <Error message={error.message} onRetry={refetch} />;
  
  return <Content data={data} />;
}
```

### 乐观更新示例

```typescript
function ToggleTodo() {
  const queryClient = useQueryClient();
  
  const mutation = useMutation({
    mutationFn: (id: string) => toggleTodo(id),
    // 乐观更新
    onMutate: async (id) => {
      await queryClient.cancelQueries({ queryKey: ['todos'] });
      const previousTodos = queryClient.getQueryData(['todos']);
      
      queryClient.setQueryData(['todos'], (old: Todo[]) => 
        old.map(t => t.id === id ? { ...t, done: !t.done } : t)
      );
      
      return { previousTodos };
    },
    onError: (err, id, context) => {
      queryClient.setQueryData(['todos'], context?.previousTodos);
    },
    onSettled: () => {
      queryClient.invalidateQueries({ queryKey: ['todos'] });
    },
  });
}
```

---

## 实施计划

### Phase 0：移动端 Bug 修复（1天）

1. **汉堡菜单与顶部重叠**: 检查并修复 Sidebar overlay 的 z-index 和 position
2. **Sidebar 遮罩层**: 检查 overlay 点击事件处理
3. **APK 图标**: 重新配置 tauri.conf.json 中的图标
4. **Sidebar 菜单点击失效**: 检查移动端点击事件冒泡问题

### Phase 1：基础设施搭建（2天）

1. **安装依赖**
   ```bash
   npm install @tanstack/react-query zustand
   ```

2. **创建 QueryClient 配置**
   - `src/lib/query.ts`
   - 在 `layout.tsx` 添加 Provider

3. **创建 Zustand Store**
   - `src/stores/appStore.ts`

4. **迁移现有 useState 到 Store**
   - sidebarOpen → useAppStore
   - selectedTags → useAppStore

### Phase 2：API 层重构（2天）

1. **类型安全增强**
   - 添加完整的 TypeScript 类型
   - API 响应类型推断

2. **错误处理统一**
   ```typescript
   // 统一的错误处理
   class ApiError extends Error {
     constructor(public code: string, message: string) {
       super(message);
     }
   }
   
   // 响应拦截器
   async function fetchWithErrorHandling<T>(url: string) {
     const response = await fetch(url);
     if (!response.ok) {
       throw new ApiError('FETCH_ERROR', response.statusText);
     }
     return response.json() as Promise<T>;
   }
   ```

### Phase 3：React Query 迁移（3天）

按依赖顺序迁移：

1. **Dashboard** - 数据最复杂，先迁移
2. **Todos** - 核心功能
3. **Plans / Targets / Milestones**
4. **Circulations**
5. **Statistics / Settings**

每个页面：
- 移除 `useState` + `useEffect` 手动加载
- 使用 `useQuery` 替换
- 使用 `useMutation` 处理创建/更新/删除
- 添加乐观更新

### Phase 4：代码质量（1天）

1. 清理所有 unused variables
2. 修复 ESLint 错误
3. 统一代码风格

### Phase 5：用户体验（1天）

1. 集成 Skeleton 组件
2. 添加 Focus 状态样式
3. 优化深色模式对比度

---

## 验收标准

### 功能验收

- [ ] 移动端汉堡菜单点击正常展开
- [ ] 移动端 Sidebar 菜单点击正常跳转（不缩回）
- [ ] APK 应用使用正确的应用图标
- [ ] 所有 view 使用 React Query
- [ ] 乐观更新正常工作

### 技术验收

- [ ] React Query Provider 正确配置
- [ ] Zustand Store 正常工作
- [ ] ESLint 0 errors
- [ ] 测试覆盖率保持 90%+

### 性能验收

- [ ] 首屏加载时间 < 2s
- [ ] 请求缓存正常工作
- [ ] 无内存泄漏

---

## 风险评估

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| React Query 学习曲线 | 中 | 详细文档和代码示例 |
| 状态迁移丢失 | 高 | 逐步迁移，每步测试 |
| 乐观更新冲突 | 中 | 使用 React Query 内置机制 |
| 破坏现有功能 | 高 | 完整测试覆盖 |

---

## 工作量

| Phase | 工作内容 | 天数 |
|-------|----------|------|
| Phase 0 | 移动端 Bug | 1 |
| Phase 1 | 基础设施 | 2 |
| Phase 2 | API 重构 | 2 |
| Phase 3 | Query 迁移 | 3 |
| Phase 4 | 代码质量 | 1 |
| Phase 5 | 用户体验 | 1 |
| **总计** | | **10 天** |

---

## 总结

激进优化方案将带来：

- **更好的开发体验** - React Query 简化数据获取
- **更高效的性能** - 内置缓存、去重、预获取
- **更好的用户体验** - 乐观更新让操作即时反馈
- **更易维护的代码** - Zustand 集中状态管理

**工作量**：约 10 天
**风险**：中等（需要完整测试）
**收益**：高
